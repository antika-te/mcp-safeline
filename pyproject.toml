[project]
name = "mcp-safeline"
version = "0.1.0"
description = "SafeLine WAF MCP Server"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.26.0",
    "httpx>=0.27.0",
]

[project.scripts]
mcp-safeline = "mcp_safeline.server:main"

[tool.uv]
package = true
